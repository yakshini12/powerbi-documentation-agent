# Diagrams

My own diagrams for this write up, written as SVG so the text stays sharp at any size.

| File | Shows |
|---|---|
| `flow.svg` | The stages behind one Skill invocation, from finding the report to opening the draft pull request |
| `distribution.svg` | The three repositories involved and why an installed Skill stays on its released bundle |
| `mcp_server.svg` | The MCP server, its tool groups and the rules around them |
| `boundary.svg` | What the model step can and cannot read, and how statements are classified |
